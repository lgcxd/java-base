java8新特性简介
    速度更快
       (HashMap变了) 数组-链表-红黑树(除了添加操作 其他的效率都高)
       (HashSet也变了) ConcurrenHashMap并发级别也变了(cas算法)效率也提高

    代码更少(增加了新的语法Lambda表达式)*****

    强大的Stream API*****

    便于并行

    最大化减少空指针异常